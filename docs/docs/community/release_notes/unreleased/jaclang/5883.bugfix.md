- **Fix: Synchronous walker execution deferred to thread pool**: `ExecutionManager.spawn_walker` and `execute_function` now run synchronously; FastAPI route callbacks are sync closures dispatched by Starlette to a thread-pool worker so blocking IO inside walkers no longer stalls the event loop.
- **Fix: Commit moved to ExecutionHandler**: `Jac.commit()` is now called inside `ExecutionHandler` so each request owns its write phase cleanly.
